#include <bits/stdc++.h>

using namespace std;

int main()
{
    // everything is same as set but stores does not store in ordered way
    // lower_bound and upper_bound functions does not work
    // rest all functions are same
    // it has better time complexity than set in most cases - O(1)
    // except some when collision happens - O(n)
    
    cout<<"Unordered Sets";
    
    unordered_set<int> ut;
    
    return 0;
}
